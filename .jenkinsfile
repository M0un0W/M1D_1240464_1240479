pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Check Out') {
            steps {
                echo 'Checking out the latest code...'
                checkout scm  // Uses the default SCM configuration in Jenkins
            }
        }

        stage('Clean') {
            steps {
                echo 'Cleaning the workspace...'
                script {
                    if (isUnix()) {
                        sh 'chmod +x ./mvnw'   // Set executable permissions for Unix
                        sh './mvnw clean'      // Run clean for Unix
                    } else {
                        echo 'Skipping permission change on Windows'
                        bat 'mvnw clean' // Run clean for Windows
                    }
                }
            }
        }

        stage('Unit Tests') {
            steps {
                echo 'Running unit tests...'
                script {
                    try {
                        if (isUnix()) {
                            sh './mvnw test'  // Run unit tests for Unix
                        } else {
                            bat 'mvnw test' // Run unit tests for Windows
                        }
                        currentBuild.result = 'SUCCESS'
                    } catch (Exception e) {
                        currentBuild.result = 'UNSTABLE'
                        echo 'Unit tests failed, marking as UNSTABLE...'
                    }
                }
            }
        }

        stage('Mutation Tests') {
            steps {
                echo 'Running mutation tests...'
                script {
                    if (isUnix()) {
                        sh './mvnw org.pitest:pitest-maven:mutationCoverage'
                    } else {
                        bat 'mvnw org.pitest:pitest-maven:mutationCoverage'
                    }
                }
            }
        }

        stage('Integration Tests') {
            when {
                expression { currentBuild.result == 'UNSTABLE' }  // Runs only if Unit Tests failed
            }
            steps {
                echo 'Running integration tests due to unit test failure...'
                script {
                    if (isUnix()) {
                        sh './mvnw verify'  // Runs integration tests for Unix
                    } else {
                        bat 'mvnw verify' // Runs integration tests for Windows
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
                // Add deployment steps here, consider using platform-specific commands if needed
            }
        }

        // stage('Smoke Tests') {
        //     steps {
        //         echo 'Running smoke tests...'
        //         // Add smoke test steps here
        //     }
        // }
    }

    post {
        success {
            echo 'Pipeline completed successfully!'
        }
        unstable {
            echo 'Pipeline completed with issues in unit tests, integration tests were executed as a fallback.'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}
